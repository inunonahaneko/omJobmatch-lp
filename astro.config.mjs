import { defineConfig } from "astro/config";

export default defineConfig({});
import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://inunonanahenko.github.io",
  base: "/omJobmatch-lp",
});